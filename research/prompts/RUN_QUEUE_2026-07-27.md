# Claude Code Run Queue — 2026-07-27

> **How to use:** paste THIS file into a fresh Claude Code session in `~/Desktop/Betting Fund Project`. Work in order, reading each prompt from `research/prompts/` and executing it fully — including its STOP-at-gate rule — before moving on. Tasks marked **parallel-safe** can be fanned out to separate sessions.
>
> Context: `STATUS_REASSESSMENT_2026-07-27.md` (repo root) and `research/RUN_SUMMARY_2026-07-26.md`.

## What changed yesterday, in one paragraph
Six KILLs, three integrity fixes, zero ADVANCEs — then a second wave that recovered 16 orphaned P-017 positions (exposing a second settler defect nine seconds after the restart), re-derived both live gates off hand-typed numbers, and discovered P-022's pod had been **running dead for three days**, structurally incapable of quoting. **Every gate now reads zero, and every zero is more honest than the number it replaced.** The Tier-1 research queue is empty. **P-022 is the entire forward pipeline, it is live, and it has never emitted a quote.**

## Global guardrails
- **Paper/demo only. No real orders, ever. No deploys** — `scripts/deploy.sh` is Sam's call, even when a task finds something that obviously wants deploying. Collect deploy items into the summary.
- **Do not change any P-022 parameter.** Band, offset, caps and window are frozen by a locked rule; changing any resets T to 0 under a new pod ID.
- **Do not re-scope any gate.** P-001 (scenario D), P-015, P-017 (settled tournaments), P-022 (T=14) are all locked or freshly re-derived. Produce evidence; Sam decides.
- **Commit as you go**, not at the end (`scripts/check_research_committed.sh` enforces it). Message: `research: <task> <verdict>`.
- Statistics: event/tournament-clustered CIs, never per-contract. **Two-sided quotes only** — never bare mids or asks on empty books. **Check anchor contemporaneity in every maker replay** (make-cut's "48h anchor" was a median 68h-old price and produced a pure +9.5¢ artifact).
- **Kalshi:** shared 2 req/s per-IP across all sessions and the live VPS. Cache everything. `settlement_value_dollars` **is** the realised payout. `result="scalar"` = **dead-heat split, not a void**. Settled markets carry **`status="finalized"`, not `"settled"`**; top-N sit `status="active"` with empty `result` for ~a day post-event — **never read "no result yet" as "void".**
- **Never touch live `data/` trade history** except where a task explicitly authorises a backed-up, allow-listed, count-asserted correction (Task 3 item 1 only).

## The queue

| # | Task | Prompt file | Effort | Parallel-safe |
|---|---|---|---|---|
| **1** | **P-022 first-quote watch — CRITICAL** | `PROMPT_P022_First_Quote_Watch.md` | short, then waits on the market | no — run first |
| **2** | **P-022 widen the backtest backwards** | `PROMPT_P022_Widen_Backtest.md` | medium | yes |
| **3** | **Corrections backlog + droplet hygiene** | `PROMPT_OPS_Corrections_And_Hygiene.md` | medium | yes |
| **4** | **Fee table fixture + CI check** | `PROMPT_OPS_Fee_Table_Fixture.md` | medium | before #6 |
| **5** | **Data-readiness audit (P-018 / MLB props / weather)** | `PROMPT_OPS_Data_Readiness_Audit.md` | short | yes |
| **6** | **R5 friction screener → graveyard validation → hunt** | `PROMPT_R5_Friction_Screener_And_Hunt.md` | long | after #4 |

### Why this order
**1 is first because P-022 is the only edge in the fund and its failure mode is silence** — which is also its correct behaviour between tournaments. It sat dead for three days and nothing noticed. Until a live quote is observed, "T = 0 of 14" and "structurally broken" look identical from the outside.

**2 is the cheapest open lead in the folder.** History reaches back to at least 2026-05-20, not the ~1 month everyone assumed, so the sample behind our only edge can be widened for minutes of API budget. It carries a hard pre-declaration requirement — write the extension plan *before* seeing numbers.

**3 clears six owed corrections**, two of which are wrong facts sitting in documents future research will read as true (the §4.2 anchor errors and the unconditional KXLEADER split claim).

**4 before 6** because the screener consumes the fee fixture, and because `_SERIES_MAKER_FEE` has drifted four times — a wrong fee produces a *plausible* number shifted by a fraction of a cent, in exactly the range where these hypotheses live and die.

**5 is cheap insurance.** Three workstreams sit on `blocked_on: time` with nobody verifying they accumulate. Two have deadlines inside three weeks; the weather pipeline runs on sleep-prone Mac cron against a 90-day horizon where a missed week is gone forever.

**6 refills an empty funnel — with a screen in front of it this time.**

## Standing priors
- Settlement mechanics **3 for 6** — a verified mechanic is necessary, **not sufficient**.
- "We have better information" **0 for 7** — do not propose another without naming why Kalshi is not already the sharper venue.
- Maker/fade **0 for 4** once adverse selection is measured honestly.
- **A filter asserted in a docstring and applied in one call path is not a filter.** Three separate incidents on 2026-07-26 (the generic settler's pod scoping, `_close_epoch`'s field preference, `from_config`'s bankroll key) were each documented correctly and wrong in the path that mattered. Before trusting a component, enumerate every consumer.

## Summary (mandatory, last action)
Write `research/RUN_SUMMARY_2026-07-27.md`:
- One line per task: verdict (**PASS / KILL / FIXED / BLOCKED / NOT REACHED**) + the single headline number.
- **Did P-022 quote?** — first line of the summary, whatever the answer.
- Links to every REPORT.
- A consolidated deploy list, in one place.
- Decisions needed from Sam, explicitly.
- Anomalies and permanent data losses found.

Expected shape of a good run: **one verified live quote, several small fixes, one honest "the funnel is still empty."** Do not manufacture an ADVANCE.
