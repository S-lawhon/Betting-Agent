# Claude Code Run Queue — 2026-07-28

> **How to use:** paste THIS file into a fresh Claude Code session in `~/Desktop/Betting Fund Project`. Work in order, reading each prompt from `research/prompts/` and executing it fully — including its STOP-at-gate rule — before moving on. Tasks marked **parallel-safe** can be fanned out to separate sessions.
>
> Context: `STATUS_REASSESSMENT_2026-07-27.md`, `Deep Research R5 - Five-Way Hunt 2026-07.md`, `research/RUN_SUMMARY_2026-07-26.md`, `research/REPORT_P022_First_Quote_2026-07.md`.

## Where things stand
The 2026-07-26 queue ran (6 KILLs, 3 integrity fixes). The overnight R5 hunt ran (5 more KILLs, 0 candidates — the Tier-1 research funnel is empty and the exchange's categories are exhausted). Task 1 of the 07-27 queue ran and found **P-022 still cannot quote**: on OPEN markets all five Kalshi time fields collapse to a ~20-day placeholder, so the [12h, 24h] window can never open. Two further §7 defects were found and fixed behind it; the pod redeployed 2026-07-27 16:00 UTC and **still has never emitted a quote.**

**The strategic finding: the fund has never passed a forward gate, and all five now read zero. The bottleneck is observation throughput, not hypothesis supply.** Rank work by observations produced per week, not by novelty.

## Global guardrails
- **Paper/demo only. No real orders, ever.**
- **Change no gate, threshold, or locked rule.** P-001 (scenario D), P-015, P-017 (settled tournaments), P-022 (T=14) are locked or freshly re-derived. Produce evidence; Sam decides.
- **Do not change any P-022 parameter** — band (0.03, 0.12), offset +0.02, window [12h, 24h], caps 0.5%/5%/15%. Any change resets T to 0 under a new pod ID.
- **Deploys are Sam's call**, with one standing exception noted in Task 1.
- **Commit as you go** (`scripts/check_research_committed.sh`). Message: `research: <task> <verdict>`.
- Statistics: event/tournament-clustered, never per-contract. **Two-sided quotes only.** **Screen on top-of-book size, not spread.** **Check anchor contemporaneity in every replay.**
- **Fees are the binding constraint, not the tick:** `fee = ceil(0.07·C·P·(1−P))` peaks at **1.75¢/ct at P=0.50** and collapses to **0.33¢ at P=0.95**. Compute fees at the *actual traded price*, never an average.
- **Kalshi traps:** while a market is OPEN, **no time field carries the real close** — they all collapse to one fallback, and `close_time` is rewritten only at close. **Never measure a time field on a settled market and infer open-market behaviour.** Settled markets read `status="finalized"` (`status=settled` is the *filter*). Top-N sit `status="active"` with empty `result` for ~a day post-event — never read as void. `result="scalar"` = dead-heat split, NOT a void. **84.8% of open markets are `KXMVE*` parlay dust** — exclude or every count is meaningless.

## The queue

| # | Task | Prompt | Effort | Parallel-safe |
|---|---|---|---|---|
| **1** | **P-022 close-time resolution — THE blocker** | `PROMPT_P022_Close_Time_Resolution.md` | medium | no — first |
| **2** | **Gate throughput audit** | `PROMPT_OPS_Gate_Throughput_Audit.md` | medium | yes |
| **3** | **P-028 golf template sweep** | `PROMPT_P028_Golf_Template_Sweep.md` | medium+ | yes |
| **4** | **R5 follow-ups** (tick size · skewed partitions · NCAAF poller) | `PROMPT_R5_Followups.md` | short–medium | yes |
| **5** | **P-022 widen the backtest backwards** | `PROMPT_P022_Widen_Backtest.md` | medium | yes |
| **6** | **Corrections backlog + droplet hygiene** | `PROMPT_OPS_Corrections_And_Hygiene.md` | medium | yes |
| **7** | **Fee-table fixture + CI** | `PROMPT_OPS_Fee_Table_Fixture.md` | medium | before any new study |
| **8** | **Data-readiness audit** (P-018 / MLB props / weather) | `PROMPT_OPS_Data_Readiness_Audit.md` | short | yes |

**Carryover note:** tasks 5–8 were queued on 07-27 and not reached. Task 6 item 7 (deploy remnant C, the `manager/` daily-brief work) **already shipped** in the 07-27 16:00 UTC rsync — verify before repeating it.

**Superseded:** `PROMPT_R5_Friction_Screener_And_Hunt.md` — the overnight R5 hunt effectively built and validated that screen. Do not run it. Its conclusions live in `Deep Research R5 - Five-Way Hunt 2026-07.md`.

### Why this order
**1 is first because nothing else can produce a verdict.** P-022 is the only validated edge, it is the only gate that can resolve inside a month (~4–5 golf tournaments/week across 13 series), and it has been structurally incapable of quoting for four days across two failed fixes. Every other task is downstream of the fund having *any* forward evidence.

**2 tests whether the whole validation machine is sound.** If gates cannot resolve at their realised rates, that is a more important finding than any candidate — and it changes what everything else is for.

**3 is the only research worth running.** R5 hunted five new categories and found nothing, but never looked deeper into golf — the one domain that works. **~10 golf templates have never been tested**, and two carry mechanics already verified verbatim: `GOLFCATEGORYLEADER` ($1/N split — structurally identical to P-022) and `GOLFH2H` (tie pays $0, so fair(A)+fair(B) = 1 − P(tie)). Golf satisfies all six clauses of the R5 survivor profile and the harness already exists.

**7 before any new study** — `_SERIES_MAKER_FEE` has drifted four times, and a wrong fee produces a *plausible* number shifted by a fraction of a cent, in exactly the range where these hypotheses live and die.

## Standing priors
- Settlement/structural mechanics **3 for 12**. "We have better information" **0 for 7**. Maker/fade **0 for 4**. Multi-leg/structural arbitrage **0 for 5**.
- **Void/cancellation arbitrage does not exist on Kalshi by construction** — every void convention pays "last traded price" or "$1/eligible strikes", both anchored to the market's own price. Pre-screen: if a mechanic's payout is anchored to the market's own price, it is variance, not edge.
- **A settlement-rule divergence is only tradeable if both rulebooks are simultaneously live on the same underlying.** Measured incidence across 12,187 series: zero.
- **The survivor profile:** large mechanic (≥5¢) · single-leg · hold-to-settlement · tail-priced · recurring · deep at the traded leg. All three survivors satisfy all six; every kill violates at least one.
- **"A filter asserted in a docstring and applied in one call path is not a filter."** This pod has now produced that failure **three times** (generic settler scoping, `_close_epoch` field preference, `from_config` bankroll key, plus the AggregateRiskGuard that was never constructed while §11c recorded it as satisfied). Before trusting a component, enumerate every consumer and check the path that actually runs.

## Summary (mandatory, last action)
Write `research/RUN_SUMMARY_2026-07-28.md`:
- **First line: did P-022 quote?** Whatever the answer.
- One line per task: verdict (**PASS / KILL / FIXED / BLOCKED / NOT REACHED**) + the headline number.
- Links to every REPORT. A consolidated deploy list. Decisions needed from Sam, explicitly. Anomalies and any permanent data loss.

Expected shape of a good run: **P-022 finally quoting, an honest throughput verdict, and one more well-evidenced KILL from the golf sweep.** Do not manufacture an ADVANCE.
