# Claude Code Run Queue — 2026-07-29 (overnight of Tue 07-28)

> **How to use:** paste THIS file into a fresh Claude Code session in `~/Desktop/Betting Fund Project`. Work in order, reading each prompt from `research/prompts/` and executing it fully — including its STOP rule — before moving on. Tasks marked **parallel-safe** can be fanned out to separate sessions.
>
> Context: `research/RUN_SUMMARY_2026-07-28.md` (read first — it supersedes `STATUS_REASSESSMENT_2026-07-27.md`), `research/REPORT_P022_Close_Time_2026-07.md`, `research/REPORT_Gate_Throughput_2026-07.md`, `research/REPORT_Data_Readiness_2026-07-27.md`.

## Where things stand

Last night's queue ran all 8 tasks: **1 FIXED+DEPLOYED, 2 KILLs, 1 INCONCLUSIVE, 3 FIXED, 1 MIXED, 0 ADVANCEs.** P-022's close-time blocker is closed and deployed (2026-07-27 17:53 UTC); the resolver reads a real round close for all three listed tournaments and the detector cleared `CLOSE_REF_PLACEHOLDER → WAITING` with **351 markets discovered, 0 unresolved**.

**The strategic finding changed.** The premise of the last two queues — *"the bottleneck is observation throughput"* — was **measured and refuted**. The fund settles plenty: 432 P-001, 54 P-014, 38 P-017, 5 P-015 in 28 days. What was missing is the path from a settlement to a *gate-countable* observation, and it was broken four separate ways. Three of those were fixed yesterday.

> **The scarce resource is not observations and not hypotheses. It is correctly instrumented observations.**

Tonight is ranked against that sentence. **No new-hypothesis research is queued** — R5 exhausted the exchange's categories and P-028 killed both golf leads, one because the mechanic does not exist. A 33rd candidate is worth less than an hour on any line of the gate table.

### The clock, which is what actually orders this queue

| deadline | what | slack |
|---|---|---|
| **2026-07-29T15:30Z** (Wed 10:30 CDT) | **P-022's first placement window opens** — AIGWO26, 24h before a 07-30T15:30Z round close | **~14h from session start.** Two prior fixes were "verified" and failed live. |
| 2026-07-30T18:30Z / 07-31T16:00Z | ROC26 and POI26 round closes | follow-on windows |
| **~2026-07-30** | **P-018's data gate opens** (27,307 ticks / 80 games; ≥10-game-day gate) | its code is **orphaned off HEAD** |
| 2026-08-17 | MLB props hits 27 clean days | **zero slack** — one missed day slips it |
| 2026-08-27 | earliest EV-Map Build 2 completion **if the 30-day clock starts tonight** | slips 1 day per day |
| 2026-10-23 | P-014 projects n=500 | needs a rule written **blind**, now |

## Global guardrails

- **Paper/demo only. No real orders, ever.**
- **Change no gate, threshold, or locked rule.** Produce evidence; Sam decides.
- **Do not change any P-022 parameter** — band `(0.03, 0.12)`, offset `+0.02`, window `[12h, 24h]`, caps 0.5%/5%/15%. Any change resets T to 0 under a new pod ID. This is binding tonight even if the pod fails to quote: **narrowing the band to make it quote is the one forbidden fix.**
- **`deploy.sh` restarts only `betting-pod-shop`.** P-022 runs as `betting-round-leader-fade` and must be restarted explicitly, or the fix ships to disk while the running process keeps the old code — the exact failure mode this workstream has been fighting.
  ```bash
  bash scripts/deploy.sh 129.212.176.202 restart
  systemctl restart betting-round-leader-fade      # NOT covered by deploy.sh
  ```
- **Deploys are Sam's call**, with one standing exception: Task 1 may deploy alarm-only changes that cannot alter a placement decision (see its prompt for the exact allowed diff surface).
- **Commit as you go** (`scripts/check_research_committed.sh`). Message: `research: <task> <verdict>` or `ops: <task>`.
- Statistics: event/tournament-clustered, never per-contract. **Two-sided quotes only.** **Screen on top-of-book size, not spread.** **Check anchor contemporaneity in every replay.**
- **Fees are the binding constraint, not the tick:** `fee = ceil(0.07·C·P·(1−P))` peaks at **1.75¢/ct at P=0.50**, collapses to **0.33¢ at P=0.95**. Compute at the *actual traded price*, never an average. The fee table is now a generated fixture — do not reintroduce a hard-coded map.
- **Kalshi traps:** on OPEN markets **no time field carries the real close**; `close_time` is rewritten only at close, so **never measure a time field on a settled market and infer open-market behaviour.** Settled markets read `status="finalized"`. `result="scalar"` = dead-heat split, **not** a void. **84.8% of open markets are `KXMVE*` parlay dust.** Settled-cache round-end: `min(close_time)` picks up **withdrawals**, `mode(close_time)` picks up **the cut** — the round end is the last cluster of any real size. **148 series tickers contain a hyphen**, so `split("-")[0]` truncates them.

## The queue

| # | Task | Prompt | Effort | Parallel-safe |
|---|---|---|---|---|
| **1** | **P-022 pre-flight + silent-failure alarm** — the window opens in ~14h | `PROMPT_P022_PreFlight_And_Alarm.md` | medium | **no — first, alone** |
| **2** | **P-022 rule decisions, written before anything settles** | `PROMPT_P022_Rule_Decisions.md` | short–medium | after 1 |
| **3** | **Cherry-pick P-018 `4ff5bea` — its gate opens ~07-30** | `PROMPT_P018_Cherrypick.md` | medium | yes |
| **4** | **EV-Map weather → droplet; stop the 1-day-per-day loss** | `PROMPT_OPS_EVMap_Hosting.md` | medium | yes |
| **5** | **P-014 decision rule, written BLIND** | `PROMPT_P014_Decision_Rule.md` | short–medium | yes |
| **6** | **P-001 post-fix admissibility rate** — decides 5 weeks vs 31 | `PROMPT_P001_Admissibility_Rate.md` | medium | yes |
| **7** | **Branch consolidation + push to origin** | `PROMPT_OPS_Branch_Consolidation.md` | short | last |
| **8** | **Gate instrumentation standard** — so this never recurs | `PROMPT_OPS_Gate_Instrumentation_Standard.md` | medium | yes |

**Not queued, deliberately:** any new-category hunt, any new golf template, `PROMPT_R5_Friction_Screener_And_Hunt.md` (superseded). If Tasks 1–8 finish early, **re-run Task 1's dry-run against the freshest ESPN data** rather than starting a ninth idea.

## Why this order

**1 is first and runs alone because nothing else has a deadline measured in hours.** P-022 is the fund's only validated edge and the only gate that can resolve inside a month. It has now been structurally incapable of quoting for five days across three fixes, two of which were declared verified and failed live. The window opens at 15:30Z. **The failure mode is silence, and silence is also correct behaviour between tournaments** — which is precisely why the alarm matters more than the fix.

**2 is second because it is the last night it can honestly be done.** `t_start_utc`, the weather-suspension rule, and posting above H=24h are all rule questions whose answers become unfalsifiable the moment a tournament settles. Writing them after the first result is fitting a rule to an outcome. The first round close is 07-30T15:30Z.

**3 and 4 are dated.** P-018's data gate opens ~07-30 with 1,688 lines of its own implementation absent from HEAD, the droplet and the test suite. EV-Map has failed **139 of 139** cron runs and loses a day of irreplaceable point-in-time quotes every day it stays on the Mac.

**5 and 8 are the direct response to last night's finding.** P-014 is the first gate in the fund's history with a real projected resolution date — and no pre-registered decision rule. Task 8 generalises: no future pod ships without a sanctioned reader and a blind rule.

**6 decides how to treat P-001.** At the all-time 14.3% admissibility rate, 200 rows needs ~31 weeks (≈ Mar 2027, past the end of the MLB season) and the pod is effectively inert. Post-fix the rate is measured at **n=1**. That one number decides whether P-001 is a live gate or a parked one, and its `blocked_on: time` label is currently dishonest either way.

**7 is last because it must capture everything above.** 66 commits — every integrity fix, every kill, the resolver, the fee fixture, the throughput instrument — exist only on an unpushed local branch named `p024-mlb-f5-research`, on a Mac whose own cron cannot read the folder.

## Standing priors

- Settlement/structural mechanics **3 for 12**, plus P-028's two leads killed on the documents rather than on data (re-derive the tally before quoting it — it has drifted before). "We have better information" **0 for 7**. Maker/fade **0 for 4**. Multi-leg/structural arbitrage **0 for 5**.
- **Forward gates resolved: zero.** No gate in this fund's history has ever passed *or* failed. Every verdict to date came from a backtest or a kill.
- **The survivor profile:** large mechanic (≥5¢) · single-leg · hold-to-settlement · tail-priced · recurring · deep at the traded leg. All three survivors satisfy all six; every kill violates at least one.
- **Compute tick + spread + fee + required edge BEFORE writing any study.** Every kill so far was knowable in advance.
- **Void/cancellation arbitrage does not exist on Kalshi by construction.** If a mechanic's payout is anchored to the market's own price, it is variance, not edge.
- **"A filter asserted in a docstring and applied in one call path is not a filter."** Five occurrences now. Before trusting a component, **enumerate every consumer and check the path that actually runs.**
- **New, from last night:** *reimplementing a sanctioned reader is how you get a second, subtly different number for a quantity meant to be unambiguous.* Call the checkpoint; never hand-roll its parser.
- **Also new:** a gate that cannot resolve is not conservative, it is inert — and a reader that cannot read must return `None`, never a stale fallback.

## Summary (mandatory, last action)

Write `research/RUN_SUMMARY_2026-07-29.md`:

- **First line: is P-022 armed to quote at 15:30Z, and what will tell us within 30 minutes if it is not?** Whatever the answer.
- One line per task: verdict (**PASS / KILL / FIXED / BLOCKED / NOT REACHED**) + the headline number.
- Links to every REPORT. A consolidated deploy list, naming **both** systemd units. Decisions needed from Sam, explicitly — carrying forward items **5, 6, 7 and 13** from `RUN_SUMMARY_2026-07-28.md` §"Decisions needed" (which Task 2 documents but does not answer), and closing **8, 9 and 10** if Tasks 3, 4 and 8 land.
- Anomalies and any permanent data loss.

**Expected shape of a good run:** P-022 armed with a loud failure path, three rule questions answered before they could be fitted to results, P-018 and EV-Map both alive before their dates, and the week's work no longer sitting on one laptop. **Do not manufacture an ADVANCE.**
