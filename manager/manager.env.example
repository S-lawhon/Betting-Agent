# Copy to manager/manager.env and fill in. chmod 600. NEVER commit the real file.
# Verify with: python3 manager/notify.py --selftest

MANAGER_SMTP_HOST=smtp.gmail.com
MANAGER_SMTP_PORT=587
MANAGER_SMTP_USER=samsonlawhon@gmail.com
MANAGER_SMTP_PASS=
MANAGER_EMAIL_TO=samsonlawhon@gmail.com

# ntfy topic acts as a shared secret — make it long and random.
MANAGER_NTFY_TOPIC=
MANAGER_NTFY_SERVER=https://ntfy.sh
