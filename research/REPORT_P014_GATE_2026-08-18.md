# P-014 Locked Gate Decision — 2026-08-18

## Outcome

P-014 is retired. The sanctioned reader reached the locked threshold and
returned `KILL`:

- admissible settled rows: 503 (threshold 500)
- terminal rows including voids: 517
- distinct games: 202
- fee-net, game-clustered edge: -2.7539 cents per contract
- standard error: 2.7637 cents
- z-score: -0.9964
- realized P&L recorded by the reader: +$1,649.56

The positive dollar P&L does not override the gate. P-014's locked claim is a
fee-net calibration edge per contract, equal-weighted across games. Section 4
of `research/P014_DECISION_RULE.md` says `n >= 500 and edge <= 0` is `KILL`.
Section 7 explicitly denies an extension in that state.

## Operational disposition

- Removed P-014 from `pods.active`.
- Set `pods.P-014.enabled: false`.
- Removed the obsolete aggregate-risk exemption.
- Registry stage is `killed`, tier is `none`, and the gate is closed.
- Existing P-014 positions remain eligible for the generic Kalshi settlement
  path; retirement prevents new entries and does not orphan open positions.

No parameter adjustment or opposite-side strategy inherits this sample. Any
successor is a new hypothesis under a new pod ID and starts at `n = 0`.
