# Claude Code Run Queue — 2026-07-26

> **How to use:** paste THIS file into a fresh Claude Code session in `~/Desktop/Betting Fund Project`. Work the queue **in order**, reading each task's prompt file from `research/prompts/` and executing it fully — including its STOP-at-gate rule — before moving on. If a task blocks (missing data, API limits, droplet unreachable), record why in the summary and move on; do not spin. Tasks marked **parallel-safe** can instead be fanned out to separate sessions, one prompt per session.
>
> Derived from `RESEARCH_TESTING_BRIEF_2026-07-26.md` (repo root), which is the current statement of where every hypothesis stands.

## Global guardrails (apply to every task, no exceptions)
- **Paper/demo only. No real orders, ever.** Nothing is added to `pods.active`. **No deploys to the VPS** — `scripts/deploy.sh` is Sam's call, even when a task finds something that obviously wants deploying. Report, don't ship.
- **No pod gets built in this queue.** Every research task ends at a committed REPORT with an explicit **KILL / ADVANCE / MARGINAL** verdict. This explicitly includes P-022: it is green-lit, but only its **settler fix and gate registration** are approved — *not* the pod.
- **Commit research artifacts as you go**, not at the end. Task 1 exists because this rule was violated and a harness was permanently lost. Commit message convention: `research: <task> <verdict>`.
- House statistics discipline: **event/tournament/day-clustered CIs**, never per-contract. Executed prints and tight two-sided quotes only — **never bare mids or asks on empty books**. Pre-event anchors on any market that trades through its own determining period.
- **Kalshi API:** shared **2 req/s per-IP** budget across all sessions *and* the live VPS services — throttle politely, cache every pull into the task's `data/` dir, resume from cache on restart. List endpoints null bid/ask (use orderbook/candlesticks). `settlement_value_dollars` **is** the realised payout. `result="scalar"` = **dead-heat split, NOT a void** (see Task 3).
- **Odds API** (`ODDS_API_KEY`): historical calls cost real credits. Sample, cache, and log spend in every REPORT.
- **Never touch live `data/` trade history.** Corrections go to new files.

## The queue

| # | Task | Prompt file | Effort | Parallel-safe | Depends on |
|---|---|---|---|---|---|
| **1** | **OPS — repo hygiene & pending-deploy audit** | `PROMPT_OPS_Repo_Hygiene.md` | ~15 min | no — run first | nothing |
| **2** | **P-017 — settlement verification** (silent-failure check, past due) | `PROMPT_P017_Settlement_Verification.md` | short | yes | droplet access |
| **3** | **P-022 — settler scalar fix + pre-registered gate** | `PROMPT_P022_Settler_Scalar_Fix_And_Gate.md` | medium | yes | informs #2 |
| **4** | **P-001 — CLV capture diagnostic** | `PROMPT_P001_CLV_Capture_Diagnostic.md` | medium | yes | Odds API budget |
| **5** | **P-026 — $0 pre-trade test, then monitor** | `PROMPT_P026_Leader_Monitor.md` | short | yes | nothing |
| **6** | **P-020 — Poly→Kalshi cross-venue backtest** | `PROMPT_P020_CrossVenue.md` | long | yes | nothing |
| **7** | **P-023 — make-cut Phase 2** (rebuild harness first) | `PROMPT_P023_MakeCut_Phase2.md` | medium+ | yes | harness rebuild |
| **8** | **P-023c — over-priced top-N flip-side** | `PROMPT_P023c_TopN_Overpriced_Fade.md` | short | after #7 | shares #7's harness |
| **9** | **P-027 — ECONSTAT prelim-vs-final flips** | `PROMPT_P027_ECONSTAT_Flips.md` | medium | yes | nothing |
| **10** | **Satellites — award ties, RT fallback/drift, WINS scanner** | `PROMPT_SATELLITES_Quirk_Census.md` | medium | yes | nothing |

### Why this order
**1–3 are integrity work and come before any new research.** Task 1 stops us losing work again. Task 2 answers whether a live pod is accruing gate progress that means anything. Task 3 fixes a settler bug that is **actively contaminating P-017's live numbers** and would erase the exact signal P-022 exists to harvest — no golf result should be trusted until it lands.

**4 is the highest-value unglamorous test we have:** P-001's CLV job captures only ~4 of 14 settled bets, which may make its 200-row gate structurally unreachable. An unfalsifiable pod is the failure mode the whole decision-rule apparatus exists to prevent.

**5 costs nothing and can kill a hypothesis in an afternoon.** Run it early precisely because it might end cheaply.

**6 has now been queued five separate times and never run.** It needs no new infrastructure. Run it.

**7 → 8 share one harness** that must be rebuilt from the surviving reports and validated against P-022's published Phase-2 numbers before it is trusted. Build it once.

**9–10** are independent mid-weight settlement-mechanic studies — the category with the only 3-for-3 record in this project.

### Two tasks that are NOT in this queue, deliberately
- **The P-022 pod build.** Green-lit, spec restored, but gated on Sam reading Task 3's output first.
- **Anything requiring a deploy.** Several tasks will identify config the droplet is not running (the `basketball_ncaaw` removal, P-017's `max_event_exposure_pct: 0.08`). Collect them into the summary as a single deploy recommendation.

## Standing prior
Settlement/mechanical hypotheses are **3 for 3** here (P-015, P-017, P-022). Behavioural and pure market-making hypotheses are **0 for 6** (P-016 v1+v2, P-019, P-025, EV-Map Build 1, and P-021/P-024 for "we have better information"). Weight your scepticism accordingly, and remember that Kalshi's books have repeatedly turned out sharper than the literature assumes.

## Summary (mandatory, last action)
Write `research/RUN_SUMMARY_2026-07-26.md`:
- One line per task: verdict (**KILL / ADVANCE / MARGINAL / FIXED / BLOCKED / NOT REACHED**) plus the single headline number.
- Links to every REPORT produced.
- **A consolidated deploy list** — everything found that the droplet is not running, in one place, for Sam to action.
- Anything needing Sam's decision, called out explicitly (at minimum: whether to approve the P-022 pod build, and any proposed re-scope of the P-001 gate).
- Anomalies: rate-limit trouble, data gaps, results surprising enough to warrant a second look.

The honest prior, as always: several of these should die at their gates. A run that produces three clean KILLs, one integrity fix, and one ADVANCE is a good run.
