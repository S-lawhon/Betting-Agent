# Overnight Research Queue — 2026-07-24

> **How to use:** paste THIS file into a fresh Claude Code session in `~/Desktop/Betting Fund Project`. Work through the queue **in order, one task at a time**, reading each task's prompt file from `research/prompts/` and executing it fully (including its STOP-at-gate rule) before moving to the next. If a task blocks (missing data, API limits), write down why in the morning summary and move on — do not spin. Alternatively, Sam may run tasks in parallel sessions; if so, each session takes one prompt file.

## Global guardrails (apply to every task, no exceptions)
- **Paper/demo only. No real orders, ever.** Nothing is added to `pods.active`; no services are deployed to the VPS tonight (unit files may be written locally).
- **Research and backtests only.** Each task ends at a committed REPORT with an explicit KILL / ADVANCE / MARGINAL verdict. **No pod gets built tonight** — pod builds require Sam's explicit approval of the gate report. (This includes P-022, which is green-lit but awaiting his sign-off — do not build it.)
- House statistics discipline: event/day/week-clustered CIs; executed prints and tight two-sided quotes only, never bare mids/asks on empty books; pre-event anchors on markets that trade through their determining period.
- Kalshi API: shared 2 req/s per-IP budget across ALL sessions and the VPS services — throttle politely, cache every pull to the task's `data/` dir, resume from cache on restart. List endpoints null bid/ask (use candlesticks/orderbook); `settlement_value_dollars` is the realized payout; `result="scalar"` = dead-heat split.
- Odds API (`ODDS_API_KEY`): historical calls cost real credits — sample, don't exhaust; log spend in the REPORT.
- Commit after each task completes (message: `research: <task> <verdict>`). Never touch `data/` live trade history.

## Already in flight — do not duplicate
- **P-025 (music charts)** is running in a separate Claude Code session on `research/prompts/PROMPT_P025_Chart_Markets.md`. **Do not touch `chart_research/`** from this queue.

## The queue

| # | Task | Prompt file | Est. effort | Depends on |
|---|---|---|---|---|
| 1 | **P-020 — Poly→Kalshi cross-venue backtest** (long overdue; highest-capacity untested candidate) | `research/prompts/PROMPT_P020_CrossVenue.md` | ~half a night's compute, mostly API pulls | nothing |
| 2 | **P-023 make-cut Phase 2 — maker-fill realism** (reuses the P-022 harness nearly unchanged) | `research/prompts/PROMPT_P023_MakeCut_Phase2.md` | short | golf_quirks_research/ (exists) |
| 3 | **P-027 — ECONSTAT prelim-vs-final flips** | `research/prompts/PROMPT_P027_ECONSTAT_Flips.md` | medium | nothing |
| 4 | **Quirk satellites — award ties, RT fallback/drift, WINS scanner** (three small censuses) | `research/prompts/PROMPT_SATELLITES_Quirk_Census.md` | medium | nothing |
| 5 | **P-026 — stat-leader split monitor** (small read-only build; good filler) | `research/prompts/PROMPT_P026_Leader_Monitor.md` | short | nothing |
| 6 | **P-024 — MLB F5/RFI thin-corner backtest** (heaviest; Odds API credits; run LAST and only if time/credits allow) | `research/prompts/PROMPT_P024_MLB_F5_Corners.md` | long | Odds API budget |

Rationale for the order: #1 has been queued for three rounds and needs no new infra; #2 is nearly free given the P-022 harness; #3–5 are independent mid-weight studies; #6 is the most expensive and explicitly budget-capped.

## Morning summary (mandatory, last action of the night)
Write `research/MORNING_SUMMARY_2026-07-25.md`:
- One line per task: verdict (KILL / ADVANCE / MARGINAL / BLOCKED / NOT REACHED) + the single headline number.
- Links to each REPORT produced.
- Anything that needs Sam's decision (e.g., an ADVANCE awaiting pod approval, an API-budget call on P-024).
- Any anomalies: rate-limit trouble, data gaps, surprising results worth a second look.
- Do NOT summarize P-025 (separate session owns it) beyond noting whether its folder shows a REPORT landed.

The honest prior, as always: several of these should die at their gates tonight. A night that produces three clean KILLs and one ADVANCE is a good night.
